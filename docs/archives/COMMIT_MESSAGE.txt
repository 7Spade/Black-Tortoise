refactor: implement event sourcing and enforce DDD boundaries

BREAKING CHANGES: None (API signatures preserved)

Core Event Sourcing Fixes:
- Fix PublishEventUseCase: append BEFORE publish (correct lifecycle)
- Fix InMemoryEventStore: query by correlationId + causationId
- Fix DomainEvent docs: clarify correlationId vs causationId
- Ensure EventStore is append-only and replay-safe

DDD Boundary Enforcement:
- Remove ALL direct eventBus.publish() from presentation layer
- Remove ModuleEventHelper publish methods (presentation must not publish)
- Presentation ONLY calls Use Cases (no state mutations)
- State changes ONLY via event handlers

Acceptance Module - Reference Implementation:
- Create ApproveTaskUseCase, RejectTaskUseCase
- Refactor AcceptanceStore to event-driven pattern
- Create registerAcceptanceEventHandlers for event subscription
- Update AcceptanceModule component to use Use Cases

Event Lifecycle (Sacred Order):
  create → append(EventStore) → publish(EventBus) → react

Benefits:
  ✅ Full audit trail (EventStore)
  ✅ State reconstruction via event replay
  ✅ Causality tracking (correlationId + causationId)
  ✅ DDD boundaries enforced
  ✅ Zero bypass of event system

Remaining Work:
- Refactor tasks, issues, QC, daily modules (follow Acceptance pattern)
- Wire event handler registration into workspace runtime
- Update integration tests

Documentation:
- EVENT_SOURCING_REFACTORING_SUMMARY.md (detailed implementation)
- EVENT_SOURCING_QUICK_REFERENCE.md (developer guide)
- EVENT_ARCHITECTURE_DIAGRAM.md (visual architecture)
- TASK_COMPLETION_SUMMARY.md (completion checklist)

Files Changed: 16 modified, 8 created
Pattern Demonstrated: src/app/application/acceptance/*

Closes: PR comment requirements
