/**
 * 單元測試樣板（尚未實作）
 *
 * 說明：
 * - 在完成與 application 層整合後，應新增測試覆蓋元件的行為（例如傳入 avatarUrl 時呈現正確的 img）。
 * - 測試應使用 Angular TestBed 或相同的團隊標準測試工具。
 */
describe('UserAvatarComponent (spec template)', () => {
  it('placeholder test - implement when wiring store/facade', () => {
    // TODO: 撰寫實際測試
    expect(true).toBe(true);
  });
});
