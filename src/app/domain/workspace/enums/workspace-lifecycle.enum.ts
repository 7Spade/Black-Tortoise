/**
 * WorkspaceLifecycle represents the lifecycle state of a workspace.
 */
export enum WorkspaceLifecycle {
  Active = 'active',
  Archived = 'archived',
  Suspended = 'suspended',
  Deleted = 'deleted',
}
