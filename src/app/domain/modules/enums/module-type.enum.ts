/**
 * ModuleType defines the type of workspace module.
 */
export enum ModuleType {
  Overview = 'overview',
  Documents = 'documents',
  Tasks = 'tasks',
  Calendar = 'calendar',
  Settings = 'settings',
}
